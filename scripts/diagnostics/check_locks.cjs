const { Client } = require('pg'); async function locks() { const client = new Client({ connectionString: 'postgresql://postgres.bxiylyhkxdyreveervhj:XA%2Ay%3F47%3Ff3YbY%243@aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=no-verify' }); await client.connect(); const res = await client.query('SELECT pg_stat_activity.pid, pg_class.relname, pg_locks.mode, pg_locks.granted FROM pg_stat_activity JOIN pg_locks ON pg_stat_activity.pid = pg_locks.pid JOIN pg_class ON pg_class.oid = pg_locks.relation WHERE pg_stat_activity.state = \'active\''); console.log(res.rows); await client.end(); } locks();  
